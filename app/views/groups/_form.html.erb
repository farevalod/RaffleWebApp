<%= form_with(model: group, local: true) do |form| %>
  <% if group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <!--<div class="field">-->
    <!--<#%= form.label :name %>-->
    <!--<#%= form.text_field :name, id: :group_name %>-->
  <!--</div>-->

  <!--<div class="field">-->
    <!--<#%= form.label :institution_id %>-->
    <!--<#%= form.text_field :institution_id, id: :group_institution_id %>-->
  <!--</div>  -->

  <table>
    <tbody>

        <tr>
          <td><%= form.label :name %></td>
          <td><%= form.text_field :name, id: :group_name %></td>
        </tr>

        <tr>
          <td colspan="2"><%= form.submit "Crear" %></td>
        </tr>

    </tbody>
  </table>

<% end %>
